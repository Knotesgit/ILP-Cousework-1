package uk.ac.ed.acp.cw2.unit;

public class ValidationServiceTests {
    /*
    // isValidAngle (multiples of 22.5°; allow tiny FP tolerance)
    @Test
    void isValidAngle_acceptsMultiplesOf22point5_andRejectsOthers() {
        assertTrue(geo.isValidAngle(0.0));
        assertTrue(geo.isValidAngle(22.5));
        assertTrue(geo.isValidAngle(67.5 + EPS));   // FP tolerance
        assertTrue(geo.isValidAngle(180.0));
        assertTrue(geo.isValidAngle(360.0));        // treated as 0°
        assertFalse(geo.isValidAngle(11.25));
        assertFalse(geo.isValidAngle(-0.0001));
    }
    */
}
